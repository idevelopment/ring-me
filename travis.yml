language: php

php:
  - 7.3.1

before_script:
  - cp .env.travis .env
  - mysql -e 'create database homestead;'
  - composer self-update
  - composer install --no-interaction --prefer-source --rewrite

script:
  - php artisan key:generate
  - vendor/bin/phpunit